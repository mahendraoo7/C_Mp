#include <stdio.h>
  void greet();
  
  void sum(int x,int y)
   {
   printf("sum of %d and %d is %d",x,y,x+y);
   }
   
  void main ()
  {
   int a,b;
   scanf("%d %d ",&a,&b);

    sum (a,b);
	
   }	