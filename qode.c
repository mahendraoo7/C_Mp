#include <stdio.h>
#include <string.h>
  void main ()
   {
   int i,j,n;
   char a[50];
   char b[50];
 
     scanf("%s",&a);
	 scanf("%s",&b);
	 
   for( i=0;i<=a[i]!='\0';i++);
    a[i] =' ';
    i++;	
   for(j=0;j<=b[j]!='\0';j++)
   { 
     
     a[i]=b[j];
     i++;
	 
   }
      a[i]='\0';	  
	  puts(a);
   }