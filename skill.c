#include <stdio.h>
#include <string.h>
  void main ()
  {
   int i,j,n;
   char a[50];
   char b[50];
 
     gets(a);
	 gets(b);
     
   for( i=0;i<=a[i]!='\0';i++);
   
   for(j=0;j<=b[j]!='\0';j++)
   {
     a[i]=b[j];
     i++;
   }
	
	  puts(a);
	
	}
   
   
   
   